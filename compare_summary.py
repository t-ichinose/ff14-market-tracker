# -*- coding: utf-8 -*-
import json

local_html = open('docs/index.html', 'r', encoding='utf-8').read()
github_html = open('docs/index_github.html', 'r', encoding='utf-8').read()

print("============================================================")
print("  GitHub公開版 (旧) vs 現在のローカル版 (新) の技術的比較")
print("============================================================")

print("""
【1. 表示構造とDOM総数の違い】
・GitHub公開版 (旧):
    - 表示単位: データセンター単位 (Elemental, Gaia, Mana, Meteor の 4カラム)
    - 画面全体の総カード数: 4 DC × 約90件 ＝ 約 360 枚のカード
    - 全4DCのカードDOMが初回読み込み時に一括生成済み。

・現在のローカル版 (新):
    - 表示単位: ワールド単位 (Anima, Asura, Chocobo, Hades, Ixion, Masamune, Pandaemonium, Titan の 8カラム)
    - 画面全体の総カード数: 8 ワールド × 110件 ＝ 880 枚のカード
    - 1回のレンダリングで生成されるHTMLノード数が旧版の【2.4倍以上 (10,000ノード超)】。

【2. DC切り替え動作の違い (超重要)】
・GitHub公開版 (旧):
    - 初回ロード時に全4DCのDOMを一度生成し、画面上に横に並べて保持。
    - DCタブ（Elemental / Gaia / Mana / Meteor）を押した時は、JSで画面を横移動（`scrollIntoView`）するだけ。
    - ★ DOMの破棄・再生成が 0 回のため、DC切り替えが瞬時（0ミリ秒）。

・現在のローカル版 (新):
    - DCタブを押すたびに、切り替え先DCの全8ワールド × 110件 (計880枚) のHTML文字列を一から計算し直し、`innerHTML` で全DOMを破棄して全再構築している。
    - ★ DC切り替えのたびに 880枚のカード構築＋計算が走るため、数秒〜の目立った描画遅延が発生する。

【3. ソート・フィルタリング計算コストの違い】
・GitHub公開版 (旧):
    - DC全体でまとめられた平坦な配列をソートするだけ (1DCにつき 1回のソート)。

・現在のローカル版 (新):
    - 各アイテム内の `item.worlds[worldName]` 辞書データを探索し、8つのワールドそれぞれに対して個別ソートと指標構築を実施 (1DCにつき 8回の個別ソートとメトリクス再算出)。
""")
